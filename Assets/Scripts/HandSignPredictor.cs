using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class HandSignPredictor : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void StartPredicting()
    {
        //Debug.Log("Commencing Predicting");
        //string line;
        //ModelInput input;
        //ModelOutput result;
            
        //string[] columns = line.Split(',');
        //input = GetModelInputFromString(columns);
        //// Load model and predict output of sample data
        //result = ConsumeModel.Predict(input);
        //DisplayResults(result, columns[columns.Length - 1]);
            
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
